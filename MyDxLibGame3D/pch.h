#pragma once
// 以下インクルードファイル.
#include "DxLib.h"

#include "Sound.h"


// 以下固定変数.

// スクリーン画面変数.
const int screenW = 640;     // 画面の解像度(横幅).
const int screenH = 480;     // 画面の解像度(縦幅).
const int screenG = 16;      // 画面のカラービット数.